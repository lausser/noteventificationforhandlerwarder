## 1. Preparation and inventory (establish expert-level test setup)

- [x] 1.1 Read MORE-TESTS-GROK.md fully and confirm the current state of checked vs unchecked items against the actual test files. Explicitly map which existing tests exercise which runtime layers (loading, runtime/bootstrap, orchestration/flow, resilience, contracts per extension point, loggers, cross).
- [x] 1.2 Review existing test helpers (test_builtin_plugins_focus.py, test_delivery_resilience.py, test_nostr.py, test_runtime_foundation.py, _new_forwarder/_formatted_event/_new_runner helpers, OMD_ROOT fixtures, pythonpath trees, server_fixture usage) and note reusable patterns. Confirm that the pythonpath trees will be used to prove uniform treatment of shipped vs. own modules.
- [x] 1.3 Identify any immediate source defects (telegram hat_id/request_parms, DecidedEvent missing _is_heartbeat, baseclass timeout exception type) that will need minimal fixes to make tests pass. Note rabbitmq-related defects as out of scope.
- [x] 1.4 Define the target flat naming convention for both siblings (consistent layer prefixes: loading_resolution, runtime_*, orchestration_*, resilience_*, contracts_formatters/forwarders/reporters(nf-only)/deciders/runners, loggers, integration_*). Confirm that eventhandler will not have any reporter file or dummy reporter tests.
- [x] 1.5 Inventory the existing test *functionality* that must be preserved (every code path, contract surface, edge, resilience behavior, loading rule, enrichment, reporter line, cross-handoff shape, logger message branch, etc.). This is the preservation contract: names and locations may change; the exercised logic must not be lost.
- [x] 1.6 Ensure a clean Python environment with pytest available; confirm that `pytest.importorskip` works for pynostr when the package is absent. (pika is not relevant — RabbitMQ forwarder is out of scope for this project.)

## 2. Establish the expert-level flat layer structure (both siblings)

- [x] 2.1 Create the target layer-named test files for notificationforwarder (flat): test_loading_resolution.py, test_runtime_config_paths.py, test_orchestration_flow.py, test_resilience_spool.py, test_contracts_formatters.py, test_contracts_forwarders.py, test_contracts_reporters.py (builtin naemonlog + preserved custom logic only), test_loggers.py, test_integration_cli.py, test_integration_cross.py (plus any supporting small files for the smokes). Add a short module-level docstring or header comment in each explaining the runtime layer it owns.
- [x] 2.2 Create the target layer-named test files for eventhandler (same prefix style, no mirroring): test_loading_resolution.py, test_runtime_bootstrap.py, test_orchestration_handle.py, test_resilience_concurrency.py, test_contracts_deciders.py, test_contracts_runners.py, test_loggers.py, test_integration_cross.py. Do **not** create any reporter file or dummy reporter content on the eventhandler side.
- [x] 2.3 Confirm the pythonpath/ trees (local/lib and lib) under both tests/ directories remain exactly as-is (or with only tiny dedicated smoke-only modules added if needed). These are first-class fixtures proving that shipped and own modules are exercised identically.
- [x] 2.4 Ensure no empty files are created on either side for concepts that do not exist in that subproject (especially: no test_contracts_reporters.py or any reporter-related file under eventhandler/tests/).

## 3. Preserve all existing test functionality (redistribute, do not lose coverage)

- [x] 3.1 Audit test_builtin_plugins_focus.py (nf): map every assertion (email/syslog/example formatter payloads, webhook modes+overrides+spool, telegram list+heartbeat, naemonlog reporter lines+defaults, logger smokes). Re-express the logic in the appropriate layer files (contracts_formatters, contracts_forwarders, contracts_reporters, loggers, resilience_spool) or keep it in the original file if it is still the best home. Rabbitmq cases must be removed or skipped. All assertions must continue to run.
- [x] 3.2 Audit test_builtin_plugins_focus.py (eh): map every assertion (decider branches, runner command rendering, baseclass handoff/concurrency/timeout/forwarder-error isolation, logger fallback). Re-express into the eh layer files (contracts_deciders, contracts_runners, orchestration_handle, resilience_concurrency, loggers). All assertions must continue to run.
- [x] 3.3 Audit test_reporter.py: preserve the naemonlog default/failure-suffix behavior, the ticketsystem custom reporter + forwarder_report_payload precedence (from event vs reporter_opts), and the full vong/ticketsystem + naemonlog end-to-end runs. Move naemonlog-focused logic primarily into test_contracts_reporters.py; keep the heavy custom-reporter integration runs in test_reporter.py or a dedicated integration location. No new "own reporter" smoke is required.
- [x] 3.4 Audit test_classes.py (nf) and test_classes.py (eh): preserve the example component + baseclass surface tests (no_more_logging isolation, run_result True/False/None, concurrent lock, forwarder failure isolation, timeout, signature file, etc.). Re-express the logic into the appropriate runtime/orchestration/resilience layer files or keep a slim top-level version. All assertions must survive.
- [x] 3.5 Audit the unittest-style test_logger.py files (both siblings): preserve the deep Text/Json logger contract coverage and the structured-field expectations. Re-express as pytest functions inside the new test_loggers.py files for style consistency. All message rendering, exception, and json-key assertions must continue to run.
- [x] 3.6 Audit test_notify.py (eh) + any symmetric cross-handoff tests: preserve the two core scenarios (runner success → properly shaped EVENTHANDLER notification through nf; decider discard → no forwarder activity). These become the seed for test_integration_cross.py on both sides.
- [x] 3.7 Audit test_runtime_foundation.py, test_delivery_resilience.py, test_discard.py, test_formatter.py, test_paths.py, etc.: confirm their logic is either left in place (deep files are allowed) or redistributed into the layer files. Every currently exercised path must still be exercised.
- [x] 3.8 After any move or re-expression, run the affected test modules to prove that the original functionality is still present and green.

## 4. Must-have: Forwarder transport coverage (notificationforwarder, into contracts_forwarders + resilience)

- [x] 4.1 Implement email forwarder tests (submit/forward paths): smtp success (html+text), html-only, text-only, missing-body fallback, smtp exception, failure-spools. Use mocks on smtplib.SMTP. Place in test_contracts_forwarders.py (or a small dedicated test_email.py if preferred for readability). Assert return values, logs, and spool side effects. **Known bug**: assert that error logs on SMTP failure go to the forwarder's logger (`logger.critical`), not the root logger (`logging.critical` as currently written at email/forwarder.py:34). This test will fail initially and pass after the fix.
- [x] 4.2 Implement syslog forwarder tests: udp success, tcp protocol, facility/priority normalization (local0/log_local0, info/log_info), invalid facility/priority fallbacks, submit exception, failure-spools. Mocks on SysLogHandler. Place in test_contracts_forwarders.py.
- [x] 4.3 Implement telegram forwarder tests: submit success (mock requests.get 200), http failure (non-200), timeout status (408/504), list payload all-success and partial-failure, heartbeat-no-spool vs non-heartbeat failure-spools, and actual-call-crash observation/fix for hat_id + request_parms. Place in test_contracts_forwarders.py + test_resilience_spool.py as appropriate.
- [x] 4.4 Verify all new transport tests use pytest.importorskip or mocks so they run without real optional packages. Assert observable outcomes (return values, critical/warning logs, spool rows or their absence, side-effect files). RabbitMQ is deliberately excluded.

## 5. Must-have: Runtime resilience gaps (into test_resilience_spool.py + test_orchestration_flow.py)

- [x] 5.1 Add webhook heartbeat failure no-spool vs non-heartbeat failure spools tests (contrast via forward() + is_heartbeat). Assert zero spool rows for heartbeat, durable rows for normal events.
- [x] 5.2 Add probe-gated flush tests: flush skipped when probe() returns False and spool non-empty; flush runs when probe() returns True; flush runs for forwarders without a probe() method (e.g. webhook).
- [x] 5.3 Add enrichment tests (enrich_raw_event / equivalent): strips unexpanded macros ($HOSTNAME$, bare $), preserves nested dict/list structures, injects omd_site / omd_originating_host / omd_originating_fqdn / omd_originating_timestamp. Can live in test_orchestration_flow.py or test_resilience_spool.py.
- [x] 5.4 Add formatter failure tests: incomplete formatted event (payload but no summary or vice versa) aborts with critical "formatted event incomplete" and no delivery/spool; formatter exception aborts with critical and no spool; missing formatter module aborts gracefully with "found no formatter module".
- [x] 5.5 Add spool replay edge tests: discarded-during-replay event is deleted with "delete trash event" log; persistent failure keeps the row and logs "event stays in spool" with loop termination on unchanged count; spool init failure returns False with critical "delivery failed and event could not be persisted"; flush beyond batch limit (>10 events, fetch_batch limit=10) computes correct attempted/recovered/stayed/deleted_trash counts and triggers stuck-detection; **formatter returns None during replay** (format_event fails on a spooled raw event) → event deleted as trash with "could not format spooled event" + "delete trash event" logs and correct deleted_trash count in the replay summary. Reuse direct sqlite patterns from existing test_delivery_resilience.py.
- [x] 5.6 Add SpoolStore direct tests (test the SQLite-backed spool layer in isolation, not through forwarder integration): enqueue with non-serializable data (verify json.dumps failure is surfaced); fetch_batch with limit=0 returns empty, limit > total returns all rows, default limit=10 returns at most 10; delete with non-existent event_id is a no-op (no crash); prune_expired at exact boundary (event whose timestamp is exactly max_spool_minutes old vs one second older — only the truly expired row is deleted); count returns accurate row count after enqueue/delete/prune; decode round-trips a raw_event through json.dumps/json.loads correctly; concurrent enqueue from two threads does not corrupt the database. Place in test_resilience_spool.py.

## 6. Must-have: eventhandler deciders, runners, and baseclass (into contracts_deciders/runners + orchestration_handle + resilience_concurrency)

- [x] 6.1 Add default decider boundary tests: host downtime loud discard, service downtime loud discard, attempt=2 loud discard with "restart ... did not help", attempt=3 or unhandled silent discard, attempt=1 non-OK no-downtime proceeds.
- [x] 6.2 Add omd_site_self_heal decider tests: host/service downtime loud discard, attempt=2 loud discard, attempt=3 loud discard (policy difference from default), attempt=1 produces user=site and check_omd --heal payload.
- [x] 6.3 Add runner command rendering tests: ssh minimal (hostname only), ssh full options, ssh payload overrides runneropt, nsc_web with/without arguments, nsc_web password quoting (single quotes), nsc_web payload overrides init, bash basic wrapping and payload command override. **Known bug**: SshRunner.__init__ references `resolve_identity_file` (ssh/runner.py:10) which is not imported — assert that instantiation either succeeds (after fix) or raises a clear NameError/ImportError, documenting the defect.
- [x] 6.4 Add baseclass path tests: timeout decorator uses correct exception type (not notificationforwarder copy-paste), DecidedEvent initializes _is_heartbeat so getter/setter do not raise, duplicate is_complete() still works, special SERVICEDESC early return bypasses decider/runner, run()=False yields success=False + critical "run failed" + no handoff, run() returning a command string exercises Popen/communicate/wait and returns stdout/stderr/success, overwrite_attributes from payload wins over __init__/runneropt, forward event shape includes NOTIFICATIONTYPE=EVENTHANDLER / author / eventhandler_success / NOTIFICATIONCOMMENT with stdout/stderr + state mapping, handoff is skipped when success=None (no_more_logging path).
- [x] 6.5 Add cross-project integration tests: runner success + forwarder 500 produces failure notification payload + "eventhandler ... failed" log while runner still logs success; decider discard means no forwarder is invoked and no notification log file activity occurs. These become the core of test_integration_cross.py.

## 7. Must-have: Nostr gaps (into test_contracts_forwarders.py + test_nostr.py preservation)

- [x] 7.1 Add nostr tests for: missing p tag fails with recipient mention and spools via forward(); host-only event uses "Service: -" and host/state-only tags; alternate field aliases still produce correct body/tags; npub1... in p tag is normalized to hex before encryption; nsec never appears in logs on success/failure; relay publish failure via forward() spools the event.
- [x] 7.2 Ensure all nostr tests use pytest.importorskip("pynostr") and mocks so they are safe in minimal CI. Preserve all existing test_nostr.py functionality.

## 8. Should-have items (high-value edges, placed in the correct layer files)

- [x] 8.1 Extend email/syslog/nostr formatters with edge input tests (host-only, acknowledgement blocks, multiline output, extra_payload_attributes, custom tags JSON, empty-field placeholders, malformed tags handled gracefully). Place in test_contracts_formatters.py.
- [x] 8.2 Extend webhook tests: 201/202 accepted as success, insecure=yes passes verify=False, url override from event.forwarderopts, headers merge, timeout exception returns False, Content-Type precedence, raw mode with list payload, **URL mutation side effect** (after one event sets event.forwarderopts["url"], self.url is permanently mutated at webhook/forwarder.py:65 — subsequent events without URL override use the mutated URL; assert this behavior and document whether it is intentional), **header case-insensitive merging** (Content-type vs content-Type vs CONTENT-TYPE all prevent the default Content-Type from being added at line 77). Place in test_contracts_forwarders.py or keep/extend test_webhook.py.
- [x] 8.3 Extend oauth2webhook tests: concurrent acquire_token, token_scope, insecure applies to token endpoint, 401 after valid token spools and retains cache, malformed token JSON critical-logs with no cache write, logger sync before super, missing expires_in defaults to 3600 with correct expires_at. Place in contracts or keep/extend test_oauth2webhook.py.
- [x] 8.4 Add reporter tests focused on builtin naemonlog (missing command_file + no cfg = silent no-op, explicit command_file appends LOG line, default CONTACTNAME=GLOBAL, host vs service default notification command, unknown reporter critical-logs but forwarding unaffected, pure no-config-anywhere silent no-op). Also preserve the existing ticketsystem custom reporter + report_payload precedence logic. All in test_contracts_reporters.py + test_reporter.py as appropriate. No new own-reporter smoke.
- [x] 8.5 Add logger CLI and message-branch tests. Place in test_loggers.py. The TextLogger _build_message method has ~30 distinct branches; the following specific message strings must each have a dedicated test asserting the exact rendered text:
  - "flush probe failed" — forwarder with probe() returning False during flush
  - "database flush+resubmit failed" — exception during flush body
  - "found no formatter module" — unknown formatter name requested
  - "spool replay summary" with all 4 counts (attempted, recovered_count, stays_in_spool_count, deleted_trash_count) plus dropped_count
  - Each spool action type: "delete spooled event" (action=delete), "event stays in spool" (action=stays_in_spool), "delete trash event" (action=delete_trash), "could not format spooled event" (action=could_not_format), "dropped outdated events" (action=dropped)
  - "concurrent flush suppressed" — second flush attempt while first holds lock
  - "spooled events to be re-sent" — flush finds spooled events
  - "spooled events could not be submitted" — stuck detection triggers
  Additionally: nf --logger json success/failure/spool/exception paths produce expected structured fields (event_host_name, event_service_name, msg.status, msg.spooled, msg.exception.trace, msg.forwarder_name, msg.split_count, msg.attempt); eh --logger json success and fallback paths; JsonLogger _add_event_info and _add_raw_event_info helpers produce correct keys for FormattedEvent vs DecidedEvent context.
- [x] 8.6 Add runtime foundation extended tests (env var fallbacks, --forwardertag suffix paths, lockfile naming rules, component loader explicit dotted + missing forwarder + reporter explicit class, forward multiple split events + mixed outcomes). Place in test_runtime_* and test_orchestration_flow.
- [x] 8.7 Add discard extended tests (DOWNTIMEEND silent, DOWNTIMECANCELLED loud, loud discard without summary dumps raw). Place in test_orchestration_flow.py or test_discard.py.
- [x] 8.8 Add alertmanager_servicenow formatter tests as pythonpath plugin example (empty alerts silent discard, payload as string via ast.literal_eval, node fallbacks, status mapping). Demonstrate uniform "own module" style. Place in test_contracts_formatters.py.
- [x] 8.9 Add eventhandler runner outcome / decider extended / baseclass extended tests (runner returns False/None, subprocess timeout, concurrent handle via subprocess, default decider SOFT+attempt1, runneropt precedence, no --forwarder means no notification, handoff payload shape, json logger on run failure, enrich omd metadata, incomplete DecidedEvent aborts, loud discard during handle). Place in the eh contracts/orchestration/resilience files.
- [x] 8.10 Add CLI smoke tests (nf --help and eh --help exit 0; missing required --forwarder/--runner produce non-zero with actionable message). Place in test_integration_cli.py.

## 9. Create the agent-friendly "own module" contract smokes (the expert templates)

- [x] 9.1 Create a small set (7-8 range is fine) of tiny, pristine contract smoke tests. They must be deliberately minimal, use the exact same `new()` / handle() entry points and pythonpath fixtures as real user modules, and be heavily commented with blocks like "# AI-AGENT TEMPLATE: when a user says 'I need a formatter/forwarder for Ticket System XY', copy this structure, replace the external mock, keep the contract assertions."
- [x] 9.2 nf side smokes (suggested 4):
  - Own formatter smoke (minimal format_event, host vs service, missing optional macros, sets payload + summary).
  - Own forwarder smoke (submit success, submit failure → spools for non-heartbeat, heartbeat failure → no spool).
  - Combined "full target" smoke (formatter + forwarder pair, optionally touching reporter context via report_payload) — the classic ticket-system integration story.
  - (Builtin naemonlog reporter coverage lives in test_contracts_reporters.py per scoping; not required as a separate "own" smoke.)
- [x] 9.3 eh side smokes (suggested 3):
  - Own decider smoke (decide_and_prepare sets payload+summary or calls discard(silently=...)).
  - Own runner smoke (run() returns command/True/False/None, payload overrides runneropt/__init__, the three intentional outcomes).
  - Cross-handoff smoke (runner result becomes a properly shaped EVENTHANDLER notification handed to a notificationforwarder target; demonstrates the sibling relationship).
- [x] 9.4 Make sure every smoke is runnable with only the project's test fixtures and mocks. They must be the clearest possible "how to test my own module the right way" examples in the entire suite.
- [x] 9.5 Place the smokes inside or immediately next to the contracts layer files so they are discoverable ("I need to implement a new integration → look in test_contracts_*").

## 10. Defect fixes surfaced by tests (minimal, in same change)

- [x] 10.1 Fix telegram forwarder typos (hat_id → chat_id, request_parms → request_params or equivalent) so submit_one is functional; update or add tests to prove it.
- [x] 10.2 Ensure DecidedEvent.__init__ initializes self._is_heartbeat (mirror FormattedEvent) so getter/setter work.
- [x] 10.3 Ensure baseclass timeout decorator uses/returns the correct exception type (ForwarderTimeoutError or project-local equivalent) instead of referencing notificationforwarder copy-paste.
- [x] 10.4 Verify any other copy-paste or control-flow defects discovered while writing the above tests are corrected with the smallest possible edit so the new test passes. RabbitMQ-related defects are out of scope.

## 11. Verification and close-out (expert-level outcome)

- [x] 11.1 Run the full test suites for both subprojects (or at minimum the notificationforwarder and eventhandler test subsets) and confirm all new and existing tests pass.
- [x] 11.2 Run any lint/type commands that are part of the project and address new issues introduced by test or minimal source edits.
- [ ] 11.3 Update MORE-TESTS-GROK.md checkboxes for all implemented items (must-haves completed; should-haves as actually done).
- [x] 11.4 Verify the directory listing of notificationforwarder/tests/ and eventhandler/tests/ is now a clear map of the runtime layers (loading → runtime → orchestration → resilience → contracts per extension point → loggers → integration). The structure must be comprehensible to a newcomer or an agent without reading every file.
- [x] 11.5 Confirm the tiny own-module smokes are usable in isolation as templates (an agent given only one of them plus the base class should be able to write correct tests for a new external system).
- [x] 11.6 Confirm: no rabbitmq tests or references in the new coverage, no reporter file or dummy reporter tests on the eventhandler side, builtin naemonlog reporter is properly covered, all existing test functionality is preserved (even if names/locations changed), and the pythonpath fixtures are used to demonstrate uniform shipped-vs-own treatment.
- [x] 11.7 Provide a short summary of the final structure, the layer files created, the smokes added, and any source fixes. Confirm that the change satisfies the expanded requirements in the delta specs and delivers the expert-level test setup described in proposal.md and design.md.

## 12. Nice-to-have (selected high-confidence items; do as time permits)

These items are not required for the change to be considered complete. They are ordered roughly by value; do as time permits after must-haves (§4–§7) and should-haves (§8) are done.

- [ ] 12.1 Add nostr forwarder extended tests (from MORE-TESTS-GROK.md should-haves, not yet in any spec scenario): multi-relay publish (two relay URLs both receive add_relay/publish); invalid npub handling (document whether submit fails or passes through raw value); forwarder tags merge (tags from formatter payload + forwarderopts + self.tags all merged); kind override (kind in payload overrides forwarder default); formatter all-aliases (exercise every _pick alias path: HOSTNAME/HOST, SERVICEDESC/SERVICE, SERVICESTATE/HOSTSTATE/STATE, SERVICEOUTPUT/HOSTOUTPUT/OUTPUT). Place in test_contracts_forwarders.py or test_nostr.py.
- [ ] 12.2 Add telegram extended tests: chat_id from runneropt guards hat_id typo, markdown escaping behavior, missing bot_token graceful failure. Place in test_contracts_forwarders.py.
- [ ] 12.3 Add email extended tests: LONGSERVICEOUTPUT/LONGHOSTOUTPUT in text body, custom sender forwarderopt, non-default smtp_port. Place in test_contracts_forwarders.py or test_contracts_formatters.py.
- [ ] 12.4 Add syslog extended tests: custom server:port, facility=daemon alias via log_ prefix. Place in test_contracts_forwarders.py.
- [ ] 12.5 Add webhook extended tests: forward-multiple-events auth header persistence, report_payload dict result delivered to reporter, queued_events max length behavior if applicable. Place in test_contracts_forwarders.py or keep/extend test_webhook.py.
- [ ] 12.6 Add oauth2 future-limitation guards if/when implemented: basic auth token method, separate token_insecure, cache file 0o600 permissions. Place in contracts or keep/extend test_oauth2webhook.py.
- [ ] 12.7 Add plugin loading/override tests: local/lib over lib formatter and forwarder, explicit --formatter different from --forwarder, missing reporter still completes, resolve_component edge inputs (empty string, multiple dots). Place in test_loading_resolution.py.
- [ ] 12.8 Add custom pythonpath plugin contract tests: vong formatter (host DOWN, WARNING/CRITICAL, OK paths), bayern formatter payload shape, ticketsystem reporter custom output and report_payload precedence, datadup formatter dedup and split end-to-end, datapost formatter post shape, heartbeat formatter marks is_heartbeat=True. Place in test_contracts_formatters.py or test_contracts_reporters.py as appropriate.
- [ ] 12.9 Add a few missing notification target scaffolds if modules exist or as contract tests (ServiceNow, VictorOps, Splunk HEC, Remedy, SMS, Slack/Teams/PagerDuty) — only if the corresponding built-in or documented plugin is present.
- [ ] 12.10 Add performance/concurrency low-priority items only if they fall out naturally: spool high-volume replay (50+ events), oauth2 multi-tag caches (5 forwardertags, independent cache files), log rotation trigger, fetch_batch limit direct, prune expiry boundary, del after failed init db, enrich raw event direct call.
- [ ] 12.11 Add documentation contract tests: readme formatter class naming (--forwarder foo loads FooFormatter/FooForwarder), readme discard API (discard() vs discard(silently=False)), readme max_spool_minutes default (5), readme logfile_backups default (3). Place in test_loading_resolution.py or test_integration_cli.py.
